#自设计的工具库starter,enableGlobalResponse开启全局响应，enablePage开启分页配置
lms:
  global:
    enableRedis: true


spring:
  http:
    server:
      multipart:
        enable: true
      maxRequestSize: 200
      maxFileSize: 200
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/init_pro?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
  redis:
    host: 47.120.1.239
    port: 6379
    password: c219034
  mail:
    host: smtp.qq.com
    port: 465
    username: 956287248@qq.com
    password: mczgqzddheugbffd
    default-encoding: UTF-8
    properties:
      mail:
        smtp:
          socketFactory:
            class: javax.net.ssl.SSLSocketFactory
        debug: true
#开启swagger配置
swagger:
  open: true
  scanPackage: com.lms.init
  title: init-server
  version: 1.0.0
  description: 初始化项目

oss:
  #root-path: C:\Users\zdh\Desktop\test
  accessKey: LTAI5t9ghEuf5NYA6oEHVaha
  secretKey: tsEqA1P5aYzGEiLBY82tK7AqWiYo0n
  #endpoint: http://119.91.143.140:20001
  endpoint: oss-cn-hangzhou.aliyuncs.com
  localOssClient: false
  pathStyleAccess: false
  bucketName: service-edu-2000
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      logic-delete-field: deleteFlag # 全局逻辑删除的实体字段名
      logic-delete-value: 1 # 逻辑已删除值（默认为 1）
      logic-not-delete-value: 0 # 逻辑未删除值（默认为 0）


sa-token:
  # token 名称（同时也是 cookie 名称）
  token-name: lmstoken
  # token 有效期（单位：秒） 默认30天，-1 代表永久有效
  timeout: 2592000
  # token 最低活跃频率（单位：秒），如果 token 超过此时间没有访问系统就会被冻结，默认-1 代表不限制，永不冻结
  active-timeout: -1
  # 是否允许同一账号多地同时登录 （为 true 时允许一起登录, 为 false 时新登录挤掉旧登录）
  is-concurrent: true
  # 在多人登录同一账号时，是否共用一个 token （为 true 时所有登录共用一个 token, 为 false 时每次登录新建一个 token）
  is-share: true
  # token 风格（默认可取值：uuid、simple-uuid、random-32、random-64、random-128、tik）
  token-style: uuid
  # 是否输出操作日志
  is-log: true
